# distributed_system_upstart
This package provides an Ubuntu upstart service to start ROS in a distributed environment. An additional config script is supplied which will help you to install the service.
